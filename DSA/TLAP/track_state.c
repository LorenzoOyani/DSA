#include <stdio.h>

// make a conversion between a character and an int;
// track and output the changes tot eh console
int main(){

    char digits;
    char inputDigitChar= scanf(digits);

    int charState;
    if(inputDigitChar == 10){
        int number = number + 'A' -1;
        charState = number;
    }
    printf("digits %t\%d\n", charState);


}